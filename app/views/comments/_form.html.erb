<%= form_for([@post, @post.comments.build]) do |f| %>
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body, html: { cols: "80", rows: "20"} %>
  </p>
  <br>
  <p>
    <%= f.submit %>
  </p>
<% end %>
